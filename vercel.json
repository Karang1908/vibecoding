{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && mkdocs build",
  "outputDirectory": "site",
  "cleanUrls": true,
  "redirects": [
    {
      "source": "/(.*)\\.md",
      "destination": "/$1/"
    }
  ]
}